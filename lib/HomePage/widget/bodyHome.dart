import 'package:flutter/material.dart';

class bodyhome extends StatelessWidget {
  const bodyhome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
       child: Image.asset("assets/images/homeUI.png", height: 310*1),
      );
  }
}
